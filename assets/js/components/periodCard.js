export function createPeriodCard() {
  const node = document.createElement("article");
  node.className = "card";
  node.innerHTML = "<h3>Période (stub)</h3><p>Composant en attente d'implémentation.</p>";
  return node;
}
